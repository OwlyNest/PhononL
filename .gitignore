# EDK2 build output (regenerated by `build`)
Build/
Shadow/build/

# EDK2 workspace config — regenerated from BaseTools/Conf/*.template,
# see setup.ps1. Machine-specific (thread count, toolchain paths), don't track.
Conf/

# ISO staging / packaging artifacts (boo.ps1)
isodir/
esp.img
phonon.iso

# QEMU / OVMF local state
OVMF_VARS.4m.fd

# clangd / compile database
compile_commands.json
.cache/
.clangd
.vscode

# Editor / OS cruft
*.swp
*~
.DS_Store
pushphonon.ps1
SERIAL.TXT